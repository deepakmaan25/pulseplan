diff --git a/src/app/(app)/AppShellClient.tsx b/src/app/(app)/AppShellClient.tsx
index 1b4b303..f3420d1 100644
--- a/src/app/(app)/AppShellClient.tsx
+++ b/src/app/(app)/AppShellClient.tsx
@@ -21,10 +21,30 @@ import styles from "./layout.module.css";
  * Plan tab via an in-screen Plan|Board toggle (product decision). The desktop
  * sidebar lists Board separately. */
 const TABS: [BottomNavTab, BottomNavTab, BottomNavTab, BottomNavTab] = [
-  { key: "today", label: "Today", icon: <CalendarDays size={22} />, href: "/today" },
-  { key: "plan", label: "Plan", icon: <CalendarRange size={22} />, href: "/plan" },
-  { key: "board", label: "Board", icon: <SquareKanban size={22} />, href: "/board" },
-  { key: "stats", label: "Stats", icon: <BarChart2 size={22} />, href: "/analytics" },
+  {
+    key: "today",
+    label: "Today",
+    icon: <CalendarDays size={22} />,
+    href: "/today",
+  },
+  {
+    key: "plan",
+    label: "Plan",
+    icon: <CalendarRange size={22} />,
+    href: "/plan",
+  },
+  {
+    key: "board",
+    label: "Board",
+    icon: <SquareKanban size={22} />,
+    href: "/board",
+  },
+  {
+    key: "stats",
+    label: "Stats",
+    icon: <BarChart2 size={22} />,
+    href: "/analytics",
+  },
 ];
 
 function pathToKey(pathname: string): string {
diff --git a/src/app/(app)/DesktopSidebar.module.css b/src/app/(app)/DesktopSidebar.module.css
index 702df78..d2df6f3 100644
--- a/src/app/(app)/DesktopSidebar.module.css
+++ b/src/app/(app)/DesktopSidebar.module.css
@@ -6,7 +6,7 @@
   flex-direction: column;
   gap: var(--s-2);
   padding: var(--s-10) var(--s-8);
-  background: var(--bg-base);
+  background: var(--surface-1);
   border-right: 1px solid var(--divider);
 }
 
diff --git a/src/app/(app)/DesktopSidebar.tsx b/src/app/(app)/DesktopSidebar.tsx
index 1f28a8d..b1da5b7 100644
--- a/src/app/(app)/DesktopSidebar.tsx
+++ b/src/app/(app)/DesktopSidebar.tsx
@@ -22,11 +22,36 @@ export interface SidebarNavItem {
 }
 
 const NAV: SidebarNavItem[] = [
-  { key: "today", label: "Today", href: "/today" as Route, icon: <CalendarDays size={18} /> },
-  { key: "plan", label: "Plan", href: "/plan" as Route, icon: <CalendarRange size={18} /> },
-  { key: "board", label: "Board", href: "/board" as Route, icon: <SquareKanban size={18} /> },
-  { key: "stats", label: "Stats", href: "/analytics" as Route, icon: <BarChart2 size={18} /> },
-  { key: "profile", label: "Profile", href: "/settings" as Route, icon: <User size={18} /> },
+  {
+    key: "today",
+    label: "Today",
+    href: "/today" as Route,
+    icon: <CalendarDays size={18} />,
+  },
+  {
+    key: "plan",
+    label: "Plan",
+    href: "/plan" as Route,
+    icon: <CalendarRange size={18} />,
+  },
+  {
+    key: "board",
+    label: "Board",
+    href: "/board" as Route,
+    icon: <SquareKanban size={18} />,
+  },
+  {
+    key: "stats",
+    label: "Stats",
+    href: "/analytics" as Route,
+    icon: <BarChart2 size={18} />,
+  },
+  {
+    key: "profile",
+    label: "Profile",
+    href: "/settings" as Route,
+    icon: <User size={18} />,
+  },
 ];
 
 export function DesktopSidebar({
diff --git a/src/app/(app)/layout.module.css b/src/app/(app)/layout.module.css
index fcbfb5d..53e85bf 100644
--- a/src/app/(app)/layout.module.css
+++ b/src/app/(app)/layout.module.css
@@ -134,7 +134,7 @@
   flex-direction: column;
   gap: var(--s-2);
   padding: var(--s-10) var(--s-8);
-  background: var(--bg-base);
+  background: var(--surface-1);
   border-right: 1px solid var(--divider);
 }
 
diff --git a/src/app/(app)/today/TodayDesktop.tsx b/src/app/(app)/today/TodayDesktop.tsx
index 7fde095..a5f3cea 100644
--- a/src/app/(app)/today/TodayDesktop.tsx
+++ b/src/app/(app)/today/TodayDesktop.tsx
@@ -26,13 +26,7 @@ function pillarMix(posts: MockPost[]) {
 
 export function TodayDesktop() {
   const router = useRouter();
-  const {
-    posts,
-    overdue,
-    todayPosts,
-    upNext,
-    greetingSub,
-  } = useTodayData();
+  const { posts, overdue, todayPosts, upNext, greetingSub } = useTodayData();
 
   const openPost = (id: string) => router.push(`/post/${id}`);
   const mix = pillarMix(posts);
@@ -69,10 +63,7 @@ export function TodayDesktop() {
           )}
 
           {/* This week's mix */}
-          <section
-            className={styles.card}
-            aria-label="This week's pillar mix"
-          >
+          <section className={styles.card} aria-label="This week's pillar mix">
             <div className={styles.cardHead}>
               <span className={styles.cardKicker}>This week&rsquo;s mix</span>
               <span className={styles.cardMeta}>{weekTotal} posts</span>
@@ -126,8 +117,7 @@ export function TodayDesktop() {
           <div className={styles.mainHead}>
             <h2 className={styles.mainTitle}>Today&rsquo;s schedule</h2>
             <span className={styles.mainMeta}>
-              {todayPosts.length}{" "}
-              {todayPosts.length === 1 ? "post" : "posts"}
+              {todayPosts.length} {todayPosts.length === 1 ? "post" : "posts"}
             </span>
           </div>
 
diff --git a/src/app/(app)/today/TodayMobile.tsx b/src/app/(app)/today/TodayMobile.tsx
index 47d4633..08da74e 100644
--- a/src/app/(app)/today/TodayMobile.tsx
+++ b/src/app/(app)/today/TodayMobile.tsx
@@ -72,7 +72,11 @@ export function TodayMobile() {
         {!hasToday && !hasOther && (
           <EmptyState
             icon={
-              <ToneIcon icon={<Sparkles size={18} />} tone="primary" size={36} />
+              <ToneIcon
+                icon={<Sparkles size={18} />}
+                tone="primary"
+                size={36}
+              />
             }
             title="You're all clear"
             description="Nothing scheduled today. Tap + to capture an idea."
@@ -81,7 +85,9 @@ export function TodayMobile() {
 
         {overdue.length > 0 && (
           <section aria-label="Overdue posts">
-            <p className={`${styles.sectionLabel} ${styles.sectionLabelDanger}`}>
+            <p
+              className={`${styles.sectionLabel} ${styles.sectionLabelDanger}`}
+            >
               {overdue.length === 1
                 ? "1 post overdue"
                 : `${overdue.length} posts overdue`}
diff --git a/src/app/(app)/today/useTodayData.ts b/src/app/(app)/today/useTodayData.ts
index 89dea92..bac787b 100644
--- a/src/app/(app)/today/useTodayData.ts
+++ b/src/app/(app)/today/useTodayData.ts
@@ -62,7 +62,9 @@ export interface TodayData {
  * render identical data in different arrangements. Pure w.r.t. its inputs
  * (posts + referenceDate); trivially unit-testable.
  */
-export function useTodayData(referenceDate: string = DEMO_REFERENCE_DATE): TodayData {
+export function useTodayData(
+  referenceDate: string = DEMO_REFERENCE_DATE,
+): TodayData {
   const { posts } = usePosts();
 
   return useMemo<TodayData>(() => {
diff --git a/src/components/providers/LayoutProvider.tsx b/src/components/providers/LayoutProvider.tsx
index 214df2b..141f3ae 100644
--- a/src/components/providers/LayoutProvider.tsx
+++ b/src/components/providers/LayoutProvider.tsx
@@ -1,4 +1,3 @@
-
 "use client";
 
 import {
diff --git a/src/components/providers/index.ts b/src/components/providers/index.ts
index 08c0204..88e66c8 100644
--- a/src/components/providers/index.ts
+++ b/src/components/providers/index.ts
@@ -1,10 +1,6 @@
 export { AppProviders } from "./AppProviders";
 export { ThemeProvider, useTheme } from "./ThemeProvider";
-export {
-  LayoutProvider,
-  useLayout,
-  useLayoutMode,
-} from "./LayoutProvider";
+export { LayoutProvider, useLayout, useLayoutMode } from "./LayoutProvider";
 export { QueryProvider } from "./QueryProvider";
 export { ToastProvider, useToast } from "./ToastProvider";
 export type { ToastInput, ToastTone } from "./ToastProvider";
diff --git a/src/lib/layout/constants.ts b/src/lib/layout/constants.ts
index f400d43..e040ab8 100644
--- a/src/lib/layout/constants.ts
+++ b/src/lib/layout/constants.ts
@@ -39,7 +39,10 @@ export function isLayoutPref(value: unknown): value is LayoutPref {
 }
 
 /** Resolve a pref + viewport signal into a concrete layout mode. */
-export function resolveLayout(pref: LayoutPref, viewportIsWide: boolean): LayoutMode {
+export function resolveLayout(
+  pref: LayoutPref,
+  viewportIsWide: boolean,
+): LayoutMode {
   if (pref === "mobile" || pref === "desktop") return pref;
   return viewportIsWide ? "desktop" : "mobile";
 }
diff --git a/src/styles/tokens.css b/src/styles/tokens.css
index da107d2..a3da2e0 100644
--- a/src/styles/tokens.css
+++ b/src/styles/tokens.css
@@ -17,8 +17,8 @@
 .pp2 {
   /* Surfaces — cool neutral with subtle blue undertone.
      Light mode uses shadow for elevation (surfaces remain white). */
-  --bg-base: #eceef2;
-  --bg-sunken: #e2e5ea;
+  --bg-base: #e4e7ec;
+  --bg-sunken: #d8dce2;
   --surface-1: #ffffff;
   --surface-2: #ffffff;
   --surface-3: #ffffff;
@@ -36,7 +36,7 @@
   --ink-inverse: #ffffff;
 
   /* Lines */
-  --divider: rgba(10, 13, 18, 0.07);
+  --divider: rgba(10, 13, 18, 0.12);
   --divider-strong: rgba(10, 13, 18, 0.11);
   --outline: rgba(10, 13, 18, 0.1);
   --outline-strong: rgba(10, 13, 18, 0.16);
@@ -153,8 +153,7 @@
   --e-1: 0 1px 2px rgba(10, 13, 18, 0.04), 0 1px 1px rgba(10, 13, 18, 0.03);
   /* Card lift (Option B): two-layer, for full cards only — NOT small chrome. */
   --shadow-card:
-    0 1px 2px rgba(16, 20, 25, 0.05),
-    0 6px 14px -3px rgba(16, 20, 25, 0.1);
+    0 1px 2px rgba(16, 20, 25, 0.05), 0 6px 14px -3px rgba(16, 20, 25, 0.1);
   --e-2:
     0 2px 4px rgba(10, 13, 18, 0.04), 0 4px 8px -2px rgba(10, 13, 18, 0.05);
   --e-3:
@@ -195,7 +194,7 @@
 
   /* Desktop content well + sidebar geometry (from Figma desktop frames) */
   --layout-sidebar-w: 232px;
-  --layout-content-max: 1280px;
+  --layout-content-max: 1440px;
 }
 
 /* ─────────────────────────────────────────────────────────────
@@ -273,7 +272,8 @@
 
   --e-1: 0 1px 2px rgba(0, 0, 0, 0.3), 0 1px 1px rgba(0, 0, 0, 0.2);
   --e-2: 0 2px 4px rgba(0, 0, 0, 0.4), 0 4px 8px -2px rgba(0, 0, 0, 0.3);
-  --shadow-card: 0 2px 4px rgba(0, 0, 0, 0.4), 0 8px 18px -4px rgba(0, 0, 0, 0.45);
+  --shadow-card:
+    0 2px 4px rgba(0, 0, 0, 0.4), 0 8px 18px -4px rgba(0, 0, 0, 0.45);
   --e-3: 0 4px 8px rgba(0, 0, 0, 0.3), 0 12px 24px -6px rgba(0, 0, 0, 0.4);
   --e-4: 0 8px 16px rgba(0, 0, 0, 0.4), 0 24px 48px -12px rgba(0, 0, 0, 0.5);
   --e-sheet: 0 -2px 8px rgba(0, 0, 0, 0.2), 0 -16px 48px rgba(0, 0, 0, 0.4);
